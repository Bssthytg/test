~={blue}创建日期=~：2025-06-12 17:12  ~={blue}修改日期=~：2025-06-12 17:12   ~={blue}位置=~: 中国北京 ~={blue}天气=~: partly-cloudy-day ~={blue}最高温度=~: 38.6°C ~={blue}最低温度=~: 23°C ~={blue}湿度=~: 36.4%
~={green}并发（Concurrency）=~
- 并发是指在~={pink}同一时间段内=~，多个程序或进程~={pink}交替执行=~，看起来像是同时运行。
- 特点
	- 宏观上同时进行，微观上交替执行
	- 通过时间片轮转实现
	- 单CPU系统也能实现并发
	- 提高系统吞吐量和资源利用率
- 实现方式
	- 时间片轮转调度
	- 优先级调度
	- 多级反馈队列调度

~={green}并行（Parallelism）=~
- 并行是指多个任务在同一时刻~={blue}真正同时执行=~，~={blue}需要多个处理器或多核处理器的硬件支持=~。
- 特点
	- 宏观上同时进行，微观上同时执行
	- 必须有物理硬件支持，多核处理器
- 现代并行技术
	- 多核处理器，GPU并行计算，分布式计算，云计算并行

~={green}共享（Sharing）=~
- 共享是指系统中的资源可以被多个进程~={green}同时使用=~。
- 共享资源类型
	- 硬件资源
		- CPU时间
		- 内存空间
		- I/O设备（打印机、磁盘等）
	- 软件资源
		- 文件
		- 数据库
		- 共享库
	- 信息资源
		- 共享变量
		- 消息队列
		- 信号量
- 共享方式
	- 互斥共享：同一时刻只能被一个进程使用（如打印机）
	- 同时共享：可以同时被多个进程使用（如只读文件）

~={green}同步（Synchronization）=~
- 同步是指协调多个并发执行的进程，使它们在访问共享资源时保持正确的执行顺序。
- 同步的目的
	- 避免竞态条件（Race Condition）
	- 保证数据一致性
	- 实现进程间协作
- 经典同步问题
	- 生产者-消费者问题
	- 读者-写者问题
	- 哲学家进餐问题
	- 吸烟者问题

~={green}互斥（Mutual Exclusion，简称Mutex）=~
- 用于解决多个进程或线程同时访问共享资源时可能产生的冲突问题。指当一个进程访问某~={blue}临界资源=~时，另一个想要访问该临界资源的进程必须等待。当前访问临界资源的进程访问结束，释放该资源之后，另一个进程才能去访问临界资源。
- 临界资源
	- 一个时间段内只允许一个进程使用的资源称为临界资源。许多物理设备（比如摄像头、打印机）都属于临界资源。此外还有许多变量、数据、内存缓冲区等都属于临界资源。

~={green}异步（Asynchronous Programming）=~
- 异步是一种编程范式，它允许程序在等待某个操作（通常是 I/O 操作）完成时，不阻塞当前线程，而是继续执行其他任务。当等待的操作完成后，程序会通过回调、事件通知等机制来处理结果。
- 在 Python 中，异步编程通常是基于 **协程** 和 **事件循环 (Event Loop)** 来实现的非阻塞，主要通过 `asyncio` 模块。

~={green}进程（Process）=~
- 进程是程序在一次执行中的一个实例，是系统进行资源分配和调度的基本单位。操作系统会根据调度算法（如时间片轮转、优先级调度等）来决定哪个进程在何时获得CPU的执行权。
- 组成
	- 程序代码
		- 实际的机器指令，即我们编写的程序。
	- 数据
		- 包含全局变量、静态变量等程序执行时所需的数据。
	- 堆
		- 用于动态内存分配的区域，程序运行时可以根据需要申请和释放内存。
	- 栈
		- 存储局部变量、函数参数、返回地址等信息，用于管理函数调用。
	- 进程控制块（PCB）
- 特点
	- 独立性
		- 每个进程都拥有自己独立的虚拟地址空间、文件描述符、打开的文件、信号、寄存器值等资源。这意味着一个进程的崩溃通常不会直接影响到其他进程，提高了系统的健壮性。
		- 操作系统为每个进程提供了一个相互隔离的执行环境。进程之间通常不能直接访问彼此的内存空间，需要通过进程间通信（IPC）机制才能进行数据交换。
	- 动态性
		- 进程是动态创建和销毁的。它从~={green}创建、运行、阻塞、就绪、终止=~有一个完整的生命周期。
		- 进程是程序在执行过程中的一个实例，是操作系统进行资源分配和调度的基本单位。多个进程可以并发执行，即在宏观上并行执行（多核CPU），或在微观上通过时间片轮转交替执行（单核CPU）。
	- 并发性
		- 多个进程在一段时间内可以交替执行，使得宏观上看起来是同时进行的。这是操作系统的重要特性，使得系统能够同时处理多个任务，提高资源利用率和用户满意度。
	- 异步性
		- 进程以异步方式运行，即它们不需要严格按照预定的时间表进行。进程的执行速度和顺序可能因系统负载、资源可用性等因素而变化。一个进程的执行进度不会被另一个进程的同步执行所限制。
	- 结构性
	- 调度性
		- 进程是操作系统调度的基本单位。操作系统会根据调度算法（如时间片轮转、优先级调度等）来决定哪个进程在何时获得CPU的执行权。

~={green}线程（Thread）=~
- 线程是进程内的执行单元，同一进程内的线程共享内存空间。Python中可以使用`threading`模块。
- 特点
	- 共享进程的内存空间
	- 创建和切换开销较小
	- 受GIL（全局解释器锁）限制，~={green}同一时刻=~只能有一个线程执行Python字节码
		- 这意味着，即使你有多个线程，在任何一个精确的时间点上，**只有一个线程**能够获得GIL并执行Python字节码
		- 导致CPython (默认的Python解释器) 的多线程无法实现真正的并行计算
		- Python 可以通过其他方式实现并行计算
			- `multiprocessing` 模块创建的是独立的进程，每个进程都有自己的Python解释器和内存空间，因此它们之间不会受到GIL的限制，可以在不同的CPU核心上并行执行。这对于CPU密集型任务非常有效。
			- Python允许使用C/C++编写扩展模块。在这些C扩展中，可以**释放GIL**，从而在C代码中实现真正的并行。许多科学计算库（如NumPy、SciPy）就是利用C扩展来绕过GIL，从而实现高性能的并行计算。
			- 其他Python解释器，`Jython`,`IronPython`,`PyPy`,Python 3.13 版本开始提供了实验性的“自由线程”构建，允许在编译时禁用GIL。
	
~={green}携程（Coroutine Python）=~
- 协程是一种用户态的轻量级并发，也称为“微线程”或“纤程”。它的调度完全由用户（程序员）控制，而不是由操作系统控制。协程在执行过程中可以主动让出 CPU 控制权，而不是被操作系统抢占。
- 并发方式
	- 协程的切换是由程序明确控制的，一个协程只有在遇到 `await` 关键字时才会暂停，并将控制权交给事件循环，让其他协程有机会执行。
	- 协程运行在单个线程中，通过在任务之间快速切换来模拟并发，因此没有线程切换的开销，也不受 GIL 的限制。

~={green}CPU密集型任务=~
- CPU密集型任务是指主要消耗CPU计算资源的任务，程序大部分时间都在进行计算，很少等待外部资源。

~={green}I/O密集型任务=~
- I/O密集型任务是指主要时间花在等待输入/输出操作上的任务，CPU大部分时间处于等待状态。







